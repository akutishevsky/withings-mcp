# Withings API Credentials
# Get these from https://developer.withings.com/
WITHINGS_CLIENT_ID=your_client_id_here
WITHINGS_CLIENT_SECRET=your_client_secret_here

# OAuth Redirect URI
# For local development: http://localhost:3000/auth/callback
# For production: https://your-app.deno.dev/auth/callback
WITHINGS_REDIRECT_URI=http://localhost:3000/auth/callback

# Server Port (default: 3000)
PORT=3000

# Encryption Secret (REQUIRED)
# Generate with: npm run generate-secret
# Or with openssl: openssl rand -hex 32
# Used to encrypt tokens stored in the database
ENCRYPTION_SECRET=your_encryption_secret_here_min_32_chars

# Supabase Configuration (REQUIRED)
# Get these from Supabase Dashboard → Settings → API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# CORS Configuration (optional)
# Comma-separated list of allowed origins for browser requests
# Native apps (Claude Desktop) don't need to be listed here
# Example: ALLOWED_ORIGINS=https://example.com,https://app.example.com
# ALLOWED_ORIGINS=
